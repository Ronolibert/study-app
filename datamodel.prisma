type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  resetToken: String
  resetTokenExpiry: Float
}

type Folder {
  id: ID! @unique
  name: String!
  subfolder: Folder
  owner: User!
  parentFolderId: ID
}
